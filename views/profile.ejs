<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10  ">
     <div class="flex justify-end">
     <a class="bg-red-600 rounded-md px-2 py-2 mb-2 mouse-pointer" href="/logout">Logout</a>
     </div>
     <h2 class="mb-2   text-3xl text-pink-800"> Hello <%= user.name %>😸 </h2> 
     <h5>You can Create a New Post.....</h5>
     <form action="/profile" method="post">
      <textarea placeholder="Share Your Thoughts..." name="content" class="dispaly-block outline-none py-3 px-3 resize-none w-1/3 bg-transparent mt-2 border-2 border-zinc-800"></textarea>
      <input type="submit" class="dispaly-block px-5 py-2 rounded-md bg-pink-600 outline-none border-2 border-zinc-800 mt-2" value="Create New Post">
     </form>
   
     <div class="posts mt-20">
        <h5 class="text-zinc-700">Your post...</h5>
     
      <div class="postcontainer flex flex-wrap gap-4 mt-2">
        <%  user.posts.reverse().forEach(function(post){%>
           <div class="post w-1/3 p-4 border-[1px]  mb-4 bg-zinc-800 rounded-md border-zinc-800"> 
           <h4 class="text-blue-600 mb-1"><%=user.username %></h4>
           <h2 class="text-sm tracking-tighter"><%= post.content %></h2>
           <div class="buttons  mt-1 flex gap-4">
            <a href="" class="text-blue-600 mb-1"> Like </a>
            <a href="" class="text-zinc-600 mb-1"> Edit </a>
           </div>
           </div>
     <% }); %>
        
       
      </div>
    </div>
  </div>  
</body>
</html>